version: 1

sitemaps:
  website:
    origin: https://www.adobe.com
    lastmod: YYYY-MM-DD
    languages:
      default:
        source: /express/query-index.json
        destination: /express/sitemap-us.xml
        hreflang: en
        alternate: /{path}
      germany:
        source: /de/express/query-index.json
        destination: /express/sitemap-de.xml
        hreflang: de
        alternate: /de/{path}
      spain:
        source: /es/express/query-index.json
        destination: /express/sitemap-es.xml
        hreflang: es
        alternate: /es/{path}
      france:
        source: /fr/express/query-index.json
        destination: /express/sitemap-fr.xml
        hreflang: fr
        alternate: /fr/{path}
      italy:
        source: /it/express/query-index.json
        destination: /express/sitemap-it.xml
        hreflang: it
        alternate: /it/{path}
      japan:
        source: /jp/express/query-index.json
        destination: /express/sitemap-jp.xml
        hreflang: ja
        alternate: /jp/{path}
      korea:
        source: /kr/express/query-index.json
        destination: /express/sitemap-kr.xml
        hreflang: ko
        alternate: /kr/{path}
      netherlands:
        source: /nl/express/query-index.json
        destination: /express/sitemap-nl.xml
        hreflang: nl
        alternate: /nl/{path}
      brasil:
        source: /br/express/query-index.json
        destination: /express/sitemap-br.xml
        hreflang: pt
        alternate: /br/{path}
      taiwan:
        source: /tw/express/query-index.json
        destination: /express/sitemap-tw.xml
        hreflang: zh-Hant
        alternate: /tw/{path}
      china:
        source: /cn/express/query-index.json
        destination: /express/sitemap-other.xml
        hreflang: zh-Hans
        alternate: /cn/{path}
      denmark:
        source: /dk/express/query-index.json
        destination: /express/sitemap-other.xml
        hreflang: da
        alternate: /dk/{path}
      finland:
        source: /fi/express/query-index.json
        destination: /express/sitemap-other.xml
        hreflang: fi
        alternate: /fi/{path}
      norway:
        source: /no/express/query-index.json
        destination: /express/sitemap-other.xml
        hreflang: nb
        alternate: /no/{path}
      sweden:
        source: /se/express/query-index.json
        destination: /express/sitemap-other.xml
        hreflang: sv
        alternate: /se/{path}
      india:
        source: /in/express/query-index.json
        destination: /express/sitemap-other.xml
        hreflang: en-IN
        alternate: /in/{path}

  blogs:
    origin: https://www.adobe.com
    lastmod: YYYY-MM-DD
    languages:
      default:
        source: /express/learn/blog/query-index.json
        destination: /express/sitemap-blog.xml
        hreflang: en
        alternate: /{path}
      japan:
        source: /jp/express/learn/blog/query-index.json
        destination: /express/sitemap-blog.xml
        hreflang: ja
        alternate: /jp/{path}
      germany:
        source: /de/express/learn/blog/query-index.json
        destination: /express/sitemap-blog.xml
        hreflang: de
        alternate: /de/{path}
      france:
        source: /fr/express/learn/blog/query-index.json
        destination: /express/sitemap-blog.xml
        hreflang: fr
        alternate: /fr/{path}
      spain:
        source: /es/express/learn/blog/query-index.json
        destination: /express/sitemap-blog.xml
        hreflang: es
        alternate: /es/{path}
      brazil:
        source: /br/express/learn/blog/query-index.json
        destination: /express/sitemap-blog.xml
        hreflang: pt
        alternate: /br/{path}
      it:
        source: /it/express/learn/blog/query-index.json
        destination: /express/sitemap-blog.xml
        hreflang: it
        alternate: /it/{path}

  seo-templates:
    origin: https://www.adobe.com
    languages:
      default:
        source: /express/express/templates/default/metadata.json?sheet=sitemap
        destination: /express/sitemap-seo-templates.xml
        hreflang: en
        alternate: /{url}
      brasil:
        source: /br/express/express/templates/default/metadata.json?sheet=sitemap
        destination: /express/sitemap-seo-templates.xml
        hreflang: pt
        alternate: /br/{url}
      france:
        source: /fr/express/express/templates/default/metadata.json?sheet=sitemap
        destination: /express/sitemap-seo-templates.xml
        hreflang: fr
        alternate: /fr/{url}
      germany:
        source: /de/express/express/templates/default/metadata.json?sheet=sitemap
        destination: /express/sitemap-seo-templates.xml
        hreflang: de
        alternate: /de/{url}
      italy:
        source: /it/express/express/templates/default/metadata.json?sheet=sitemap
        destination: /express/sitemap-seo-templates.xml
        hreflang: it
        alternate: /it/{url}
      japan:
        source: /jp/express/express/templates/default/metadata.json?sheet=sitemap
        destination: /express/sitemap-seo-templates.xml
        hreflang: ja
        alternate: /jp/{url}
      korea:
        source: /kr/express/express/templates/default/metadata.json?sheet=sitemap
        destination: /express/sitemap-seo-templates.xml
        hreflang: ko
        alternate: /kr/{url}
      spain:
        source: /es/express/express/templates/default/metadata.json?sheet=sitemap
        destination: /express/sitemap-seo-templates.xml
        hreflang: es
        alternate: /es/{url}
      taiwan:
        source: /tw/express/express/templates/default/metadata.json?sheet=sitemap
        destination: /express/sitemap-seo-templates.xml
        hreflang: zh-Hant
        alternate: /tw/{url}
